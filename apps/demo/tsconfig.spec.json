/* Configuration used while testing. Reads the local instance of @blui-lab/angular. */
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": ["jasmine", "prismjs"]
  },
  "include": ["src/**/*.spec.ts", "src/**/*.d.ts"]
}
