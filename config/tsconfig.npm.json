/* This configuration is used in the Demo Application & reads the published instance of @blui-lab/angular, found in node_modules. */
{
  "extends": "../tsconfig.json",
  "files": [
    "./src/main.ts"
  ],
  "include": [
    "../src/**/*.d.ts"
  ]
}
